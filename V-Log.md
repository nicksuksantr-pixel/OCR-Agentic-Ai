# V-Log — OCR-Agentic-Ai

> 3-place versioning `vX.Y.Z` per Nick (2026-06-12) · no place exceeds 9, carry instead.

- v0.3.0 — auto-update fix: detection un-stuck + visible install + reliable relaunch (2026-06-14, Nick from the running installed app: "auto-update is broken — pressing update keeps the same version, can't find new updates anymore, and after updating the app closes and never reopens; check the install folder + make sure uninstall works and removes shortcuts, don't let it look like a dumb program"): **Diagnosis (read his REAL machine, 0 agents):** GitHub `/releases/latest` returns v0.2.9 correctly → server fine; but `%LOCALAPPDATA%\…\applied_update.json` = `{"tag":"v0.2.9"}` while the installed exe is **v0.2.8** — `apply_on_exit` wrote the applied tag BEFORE the detached install ran, that install never completed, and the updater floored its newer-check at `max(running, applied)` = v0.2.9 → every check read `v0.2.9 <= v0.2.9` → "up to date", refusing to re-download = **permanently stuck, unable to update**. Install was actually in-place (ONE `Program Files\OCR-Agentic-Ai`, fixed AppId), uninstaller registered (`unins000.exe`, Apps & Features), Start-menu + Public-desktop shortcuts + HKLM autostart all Inno-tracked → uninstall already correct, nothing to fix. **Fixes (Coddy-direct, 0 agents):** (1) **dropped the applied-tag floor** — new `updater.is_newer(latest, running)` is the ONLY gate (compare to the RUNNING build; `parse_version`'s fixed 3-tuple already stops the 'v0.1.5.0' padding loop the floor once guarded; `staged_setup` guards in-session re-download). A failed install now re-offers next start (self-healing); `_cleanup_legacy_applied()` deletes the poison file on start; the `applied_update.json` read/write is removed. (2) **visible install + reliable relaunch** — Setup `/VERYSILENT`→`/SILENT` (shows a progress window), relaunch from the install dir after a short settle, and `installer.iss [Run]` lost `skipifsilent` so the installer ALSO relaunches in silent mode (single-instance mutex dedups the double launch). (3) uninstall verified + documented (no change). `smoke_updater` +`is_newer` (incl. the 0.2.9→0.3.0 carry) + apply-script `/SILENT` assertion; 14/14 green. **Recovery (REQUIRED — the broken v0.2.x updater can't deliver its own fix): Nick must run `OCR-Agentic-Ai_Setup_v0.3.0.exe` MANUALLY once** (in-place upgrade, keeps all data); auto-update works after. Caveat: the install/relaunch path can't be smoke-tested headlessly — the detection fix IS unit-tested + is the confirmed root cause; the visibility/relaunch changes are inspected hardening. **Released: GitHub v0.3.0 (installer + sha256), pushed.**
- v0.2.9 — whole-sheet scan (no pre-scan crop) + blue+red offset dual grid (2026-06-14, Nick watching a LIVE scan of the real Cummins CIB sheet: "why is it cropped at the edges? scan ~6 cells with a red grid + a blue grid, overlapping, then merge & filter"): **(1) Removed the pre-scan frame crop** — `frame_interior` → `content_bbox`. Proven on the real sheet: `frame_interior` was discarding the **left 643 px (8.2%) of INK** = an entire **"VESSEL TERMINAL STRIP"** column + terminal numbers 100-194/400-449 + safety **NOTES** (external shutdown switch / line-monitoring resistor 47K / remote E-stop) = **245 confident words thrown away BEFORE scanning**. `content_bbox` keeps every inked pixel (trims only blank paper margin); empty border/zone tiles are filtered from the AI queue instead of cropped. `frame_interior` deleted (the system, per Nick). **(2) Bug fix exposed by (1):** `blank`/`line_only` were decided on words BEFORE noise-filtering while the section verdict uses words AFTER — so a border tile read as pure line-noise (`|`,`/`) skipped the `line_only` gate, then turned `unreadable` downstream and got QUEUED (the empty-border AI waste the crop used to hide). Both now use `_meaningful()`. **(3) Offset dual grid (Nick's blue+red), `imaging.offset_grid()`** — a second grid staggered half a tile so every main-grid cut sits MID-tile in the offset grid (read whole, full context, along the full cut length). **Replaces the seam-strip pass** (a full tile beats a thin strip). Word-harvest ONLY (feeds the stitch; no new sections / AI-queue — the main grid owns verdicts); sparse pass, conf-filtered ≥45 + deduped → only ADDS reads, never loses/duplicates. Live preview draws offset tiles **RED**, main **BLUE** (Nick's mental model on screen). Kept the physical-size tile — NOT Nick's literal "6 cells" (too coarse: huge tiles hit the 8000 px zoom cap so tiny text would regress). **14/14 smoke green** (smoke_grid 15/15: cut-straddling word read whole via the offset grid; queued=[]). **Contract: no schema change** — more complete `words`/`full_text`, same fields (the Heart simply sees more). Caveat: the offset grid ≈ DOUBLES tile OCR (slower; Nick "quality first"), recovery-vs-cost not yet benchmarked on the real sheet. **Lucifer audit (Nick trigger "ใช้ lucifer วิเคราะห์ว่าสมบูรณ์ไหม"): PASSED, score 4/5 — production code (offset_grid coverage, dedupe word-safety, degenerate 1×1/1-row grids, pause/cancel, no schema change, event safely ignored by watcher/API) VERIFIED correct via 105k+ fuzzed cut shapes (0 un-straddled cuts); no src change needed. It added an isolated offset-coverage smoke test (the union of tiles straddling each main cut spans the cut's full length + degenerate grids) — the SEAMWORD end-to-end test didn't isolate the offset grid → smoke_grid 15→18/18. Coddy verified the real diff (test-only, src untouched) + re-ran 18/18 on live code before merge.** **Released: GitHub v0.2.9 (installer + sha256), pushed; existing installs auto-update.**
- v0.2.8 — quality pass (2026-06-14, Nick: "จัดมาอันที่คุ้มค่าเน้นคุณภาพ"): the two quality/correctness items worth doing from the surfaced backlog (the rest are perf Nick accepts as "slower is fine", or code-cleanliness nits left as-is). Coddy-direct, 0 agents. **(1) Seam coverage:** the seam pass skipped a strip when `ink_ratio < BLANK_MAX_INK` (0.004) — but a seam strip spans the full interior length, so a single line of text crossing the cut dilutes to ~that bar and could be skipped. A dedicated lower **`SEAM_MAX_INK` (0.0010)** means only genuinely-blank seams skip; a thin crossing label is now scanned. **Safe by construction** — it only ADDS seam OCR candidates, which are conf-filtered (≥45) and de-duplicated, so no word can be lost (extra near-blank scans are cheap; "slower is fine"). NOT the risky kind of seam change — the v0.1.5 regression was a dedupe-eviction rewrite; this doesn't touch eviction. **(2) created_at consistency:** result.json carries the SAME `created_at` as the DB row (`store.job_created_at`) instead of a second `now()` at persist time, which for a multi-minute page differed by minutes — the Heart now sees one timestamp per job. Additive/contract-safe; 14/14 smoke green. **Caveat:** the seam gain is safe but UNQUANTIFIED — no real drawing was on disk to benchmark; drop one in `ผู้ใช้\` + run `tests\scan_real_sample.py` to measure. **Released: GitHub v0.2.8 (installer + sha256), pushed; existing installs auto-update.**
- v0.2.7 — UI/UX + bug pass via Lucifer (2026-06-14, "Lucifer: เทสและอัพเดท ui ux และบัค"): the Lucifer 3-agent relay (R1 5/5/4 → R2 5/5/5; verified against live code by Coddy before merge, isolated `.lucifer-wt` worktree) addressed the UI/UX + bug items the v0.2.6 Tester audit had surfaced. **(1)** A real Quit (window close with tray off, OR the tray-menu Quit) and "Install & restart now" now **warn with a themed dialog when a scan is running and cancel it first** (the pipeline checkpoints, so finished pages stay committed) — the tray-menu Quit was the primary silent-loss path because the app hides-to-tray by default. **(2)** **Offline-first language fallback:** if a selected OCR model isn't installed (e.g. `tha` missing on a portable run), the scan narrows to the languages Tesseract can actually load (an English page reads on `eng`) instead of dying; no change when all selected languages are present, and the original clear error still surfaces when none are. **(3)** the Scan tab **warns** when a selected language isn't installed (informational, not a hard block). **(4)** **Archived re-scan parity:** a re-picked archived single image prompts like a PDF page is skipped on resume (`jobs_for_exact_source(include_archived=True)` for the guard; default callers unchanged). **(5)** **Idle UI timers** (Dashboard stats + Settings update-status, every 2 s) skip their DB read / folder scan while hidden to the tray (timer stays alive); plus update-bar button tooltips. All additive, contract-safe; 14/14 smoke green. **Released: GitHub v0.2.7 (installer + sha256), pushed; existing installs auto-update.**
- v0.2.6 — Tester audit fixes (2026-06-14, "Tester"): a 3 review-only-agent audit (627k tokens, agentsUsed=3, 16 findings P0:0/P1:4/P2:6/P3:6) → Coddy verified each against live code and fixed the real ones (0 fix-agents). **P1:** (1) the inbox watcher + local API scanned without ever calling `engine.configure()` — they worked only because the GUI's ScanView configured Tesseract first; now `engine.ensure_configured()` runs at the top of every `run_job`, so all three doors are self-sufficient. (2) an empty / `(nothing readable)` Gemini answer used to mark a section `boosted` permanently and write an empty `section N:` line — a silent drop of an unread region; now it fails the boost (not requeued) and leaves the section unresolved. (3) the AI-merge idempotency guard matched the substring `\nsection N:`, so a multi-line `ai_text` containing such a line could spoof a later section's header and drop its real reading — each reading is now collapsed to ONE line before merge (DB `_merge_full_text` + file `_update_result_json`), so only real headers match (raw text kept in `ai_boosts`). (P1.4 migrate schema-stamp verified NOT a bug — `_SCHEMA`/ALTER make the DB current before `_stamp_identity`.) **P2:** API `POST /scan` rejects non-image/PDF types (shared `scan_service.SUPPORTED_EXT`) instead of junk error-jobs; the boost enqueue gate is `ai_boost_enabled AND key` (was key-only → queue swelled when Boost was off but a key existed); a rejected-key auth error refunds the daily-cap count it pre-spent. **P3:** `delete_job`'s processed-original purge matches the EXACT basename (a stem wildcard could delete a different source's clash-renamed copy); `GET /jobs?limit` clamped 1–1000; README → v0.2.6. `smoke_harden` +2 checks; 14/14 smoke green. **Surfaced (not auto-changed):** rescue ~8 Tesseract/tile (perf, "slower is fine"), quit/update-during-scan abandons the in-flight page (recoverable via resume), archived-image vs PDF-page resume inconsistency, language pre-check needs graceful fallback, created_at file↔DB skew, seam ink-threshold (unconfirmed), minor cleanups. **Released: GitHub v0.2.6 (installer + sha256), pushed; existing installs auto-update.**
- v0.2.5 — Medium backlog fixes via Lucifer (2026-06-14, Nick: "เปิด lucifer วิเคราะห์แก้โค้ด"): the **Lucifer 3-agent relay** (plan→code→3 reviewers; round 1 4/4/4 → round 2 5/5/5; verified against live code by Coddy before merge, run in an isolated `.lucifer-wt` worktree) cleared the remaining verified Medium findings from the v0.2.3 Tester audit. **(1)** OCR **language picker** in Settings (`LANGS` combos bound to `Settings.languages`, populated from `engine.available_languages()`) + a missing-language **warning banner** on Re-check, so a dev/portable run without `tha.traineddata` warns instead of throwing on the first scan (the installed .exe bundles tha → end users unaffected). **(2)** paid-tier + model **persist on change** (not only on Save) via `_persist_tier()` on the toggle + a model-menu `command`. **(3)** the Save toast now says **watch-inbox / Local API changes apply after restart** (they are read once at App init — honest messaging, the low-risk option). **(4)** `scan/service._persist` **skips queueing AI Boost when there is no Gemini key** (the local Raw Extract is still written in full and the crop is kept for a later key + re-scan) — no more undrainable boost rows. **(5)** new `tests/smoke_unrotate.py` round-trips `unrotate_box` against PIL's own `rotate(expand=True)` for 0/90/180/270 — it **confirmed the formula was already correct**, so `imaging.py` is unchanged. All 14 smoke suites green. First fully-successful Lucifer run since the v0.2.3 args bug was fixed. **Released: GitHub v0.2.5 (installer 68.2 MB + sha256 `47a89062…`), pushed `ee47c25`; existing installs auto-update.**
- v0.2.4 — local API auth (2026-06-14, Tester v0.2.3 audit High · Nick: "handle it"): the localhost API bound 127.0.0.1 but needed NO credential, on by default and running while hidden to tray, so any local process OR a malicious browser localhost-fetch could POST `/scan` (OCR any readable file) and POST `/boost/run` (spend the user's Gemini quota). Fix: a **shared token** (`store.api_token()`, generated once into the `meta` table, race-safe `INSERT OR IGNORE`) is now **required on POST routes** via the `X-OCR-Token` header (constant-time compare) — 401 otherwise; GET routes stay open (CORS is never granted, so a browser page can't read a GET response either, and POST actions are the ones with side effects). The token is published in the additive `api.auth` block of `introduction.json` + `GET /introduce` so the Heart can read and send it. **Contract — breaking for POST: Open-Claw must read the token from /introduce and send `X-OCR-Token` on POST /scan + /boost/run** (token field additive, the requirement is the breaking part). smoke_api 9/9 (new "401 without token"), regression green. Shipped within the v0.2.5 GitHub release.
- v0.2.3 — reliability hardening (2026-06-13, "Tester"+"Lucifer"; **backfilled to V-Log 2026-06-14** — it had been missing here, in `log\` and in chat_log): a Tester audit (3 review-only agents → 27 findings, P0:0/P1:4/P2:12/P3:11) → Coddy fixed 5 verified findings by hand (Lucifer's rounds were VOID on the Workflow args-string bug — see MEMORY lessons + `workflow-args-no-double-quotes`). (1) **AI-Boost answer+merge atomic** — `store.complete_boost_atomic()` writes boost_queue + sections + jobs.full_text in ONE transaction; `reconcile_boost_merges()` at each drain start re-folds any answer lost from full_text on a crash (the old 3-connection path could lose an AI reading from the Heart's canonical field forever); full_text append idempotent per section + result.json mirrored. (2) **additive secondary indexes** (idx_words_job / sections_job / boost_status / jobs_archived, SCHEMA_VERSION→3, CREATE INDEX IF NOT EXISTS) — Heart per-job reads were full scans. (3) **empty-result guard** — a 0-page/unreadable PDF no longer crashes the inbox watcher (`results[-1]`) or API /scan (`results[0]`→500). (4) **path-heal** — export_json + API /jobs/{id}/result use `resolve_job_dir()`. (5) **updater integrity hard-stop** — a release missing its `.sha256` no longer stages an unverified Setup. New `tests/smoke_harden.py` (6 checks); 13/13 green; released (commit 3f33cd7). Deferred to Backlog: API auth (→ done v0.2.4), OCR-language picker, paid-tier persist, interface-toggle apply, perf, P3 nits.
- v0.2.2 — live scan view: page preview + per-section feed + matching bars (2026-06-13, Nick on the Scan/Dashboard screenshots: "why the empty space? 40+ sections in and nothing shows; the other page has a status bar that doesn't work; they don't match; shouldn't this be on the Scan page too?"). Both empty/dead areas shared ONE cause: progress was reported per *page*, but a page is a multi-minute ~49-section job, so nothing moved within it — the Scan result box stayed empty until the whole page finished, and the Dashboard bar (reading only page-granular `progress`) sat on "starting..." at 0; the two also disagreed because the Scan tab showed section detail from the `on_progress` string while the Dashboard read a `progress` dict that never saw a section. Fixed with an additive structured **`on_event` feed** threaded `run_source → run_job → _scan` (default no-op, schema/contract untouched): it emits the upright page thumbnail, then per-section bbox + text in the same coordinate space as the scan boxes. **Scan tab reworked** — a live progress bar + a body split into a page-preview pane (current section highlighted) and a streamed-text box (each section's words appear as read; the clean stitched text folds in when the page finishes; idle shows a labelled placeholder, not a black void). **ScanController.progress** now carries section/sections (updated by a `relay_event`) so the **Dashboard** bar moves smoothly (`((page-1)+section/sections)/pages`), shows "page X/Y, section A/B" + a fraction-based ETA, and both screens share one source of truth. Then (same v0.2.2, Nick: "fix it + new UI + all backlog, build once"): **A1** AI-Boost text folded into the file's full_text (was only in `ai_boosts` → boosted sections 29/30 read as empty); **B1** API async scan (`{"async":true}` → 202 + poll); **B2** pause/cancel responsive inside the rescue loop + ETA excludes paused time; **B3** additive `no_text` flag (blank/photo vs failure); **B4** image re-scan prompt; **B5** app-themed modal dialogs replace every native messagebox. Reading-order interleave (A2) deliberately NOT shipped (benchmark showed no clean fix; word coords stay correct for Open-Claw). **Root cause of the whole arc: Nick was running a stale v0.1.5 frozen build — every symptom was a v0.1.x bug already fixed in v0.2.0/0.1 he never installed; the OCR itself is good (job_0046: 84.8% conf, 3807 words, all tables read, AI Boost fixed sections 29/30 correctly).** All 12 smoke suites green; A1 verified end-to-end on real data; built `OCR-Agentic-Ai_Setup_v0.2.2.exe` (68.2 MB, sha256 fe1a8a86…). Delivery: one fresh clean store (the two old split stores backed up then wiped) → install over v0.1.5 → GitHub release. Lessons: confirm WHICH build the user actually runs (`/health` / introduction.json) before assuming bugs are unfixed; report progress at the slow unit's granularity; keep result.json self-contained (data living only in `ai_boosts` reads as missing).
- v0.2.1 — visible, one-click updates (2026-06-13, Nick: "I can't tell if it updates — no status, no button — and it never updates itself"). The v0.2.0 updater downloaded silently and only installed on a *full* Quit, but the app lives in the tray (closing the window only hides it), so the staged update almost never applied and nothing on screen showed it. Fixed: **a prominent update bar** across the top of the window appears the moment an update is downloaded+verified — "🔄 Update vX ready to install" + **[⬇ Install & restart now]** + [Later]; the button installs immediately and relaunches (no manual Quit needed). **Settings → Updates** now shows the installed version, a **live status** (checking / up to date / downloading / ready / error) and the same Install button (enabled only when ready). The bottom status strip carries a persistent "update ready" hint. The updater gained a `state` machine + `latest_tag` + an `on_ready` callback feeding all of this, and the App **re-checks every 6 h** (not just on start) so a tray-resident app still finds updates. App background-event scheduling is now teardown-guarded. smoke_gui asserts the bar is hidden until ready then shows with the tag; 12/12 green. NOTE: because Nick's current build still has the old silent-only updater, this one is delivered by running the installer once; from v0.2.1 on, updates are visible + one-click.
- v0.2.0 — UI redesign + reliability overhaul (2026-06-13, "Tester" — Nick: "redo the whole system, it looks bad; buttons half-work; 'Arch' means nothing; open-folder is empty; the file is English but the text has Thai; tabs hang with no log; deleted files come back after updates"). 7 audit agents (4 diagnostic + 3 lane) found the root causes; all fixed in one pass. **Data layer:** ONE canonical Shared Store — dev and installed both use `%LOCALAPPDATA%\OCR-Agentic-Ai` now (they used to diverge → "open folder empty" + "deleted jobs come back"); the legacy `<project>\data` store is migrated once on first run (job_dir paths rewritten); `tessdata` is found across dirs so dev still reads `tha`; store gets WAL + 30 s busy-timeout, a lock-guarded one-time migration, and an additive `meta` table (store_uuid / schema_version / last_app_version). **Folder naming:** job folders are named from the DB id (`job_<id:04d>`, monotonic, never reused) instead of a glob of on-disk folders — this ends the `job_0002 → job_0002_1_1_1` collision chain that *was* the visible "trashed jobs coming back". **Archive/delete:** `archive_job` is idempotent (already-archived = no-op, id-qualified trash name, no re-suffix); `open_job_folder` heals a moved/stale path (no more "Location is not available"); `delete_job` is a true delete (also purges the inbox\processed original) and PIL handles are closed so rmtree always succeeds; whole-file/multi-select delete query the store by source in SQL (no silent 500-row cap). **Updater:** version compare normalised to a fixed 3-tuple + remembers the last installed tag (`applied_update.json`) so it never re-installs the same build in a UAC loop. **Thai noise:** `latin_only_page` rewritten as a character-MASS Latin-dominance test (fires on the dense drawings it was built for), a `_mostly_thai` token post-filter drops any Thai glyph that slips onto an English-judged page, the verdict is recomputed on the full stitched words, and standalone pipe/line tokens are filtered — genuine Thai pages keep `tha`. **UI (full redesign):** new shared design tokens (`src/shared/ui/theme.py`) + reusable widgets (`ActivityLog`, hover `Tooltip`) so all four tabs share one look; a **JobsController** runs every Jobs-tab operation (list / search / preview / overlay / export / delete) off the Tk thread — the "tab hangs/won't refresh" freeze is gone; a **shared Activity log** on the Dashboard + a persistent bottom status strip receive every background event (scan/watcher/API/updater/boost) with a source tag, instead of all overwriting one Scan label; Jobs has **per-row checkboxes + Delete/Archive selected + Delete whole file**, full button labels (no more clipped "Arch"), tooltips on icon buttons, and the detail action row is **disabled until a job is selected**; Settings **scrolls**, exposes the **Tesseract path + Browse + Re-check**, and rejects bad numeric input instead of a misleading "✅ Saved."; the Scan tab shows recoverable **engine/AI-key banners**. **Also:** boost drains serialized + atomic claim (no quota double-spend), tray uses its own running flag, PDF opened once per file, failed/cancelled jobs write a minimal result.json (API returns it instead of 404), `store` self-creates its dir. **Tests:** all 12 smoke suites isolate their store (temp dir — no more leaking into the real library) and pass; `smoke_gui` rewritten to assert the redesigned structure (tabs, activity-log routing, disabled-until-selected). Contract: introduce.json documents the single store + id-based folder naming + languages_used semantics (additive).
- v0.0.0 — initial project (2026-06-12): kickoff grill → CONTEXT.md · feature-based scaffold · local hybrid-ready pipeline (Tesseract tha+eng, full pass + 3×3 Sectioned Scan + stitch + dedupe) · SQLite Shared Store (jobs/sections/words/boost_queue) · CustomTkinter GUI (Scan + Jobs tabs) · smoke test passing.
- v0.0.1 — AI Boost sender (2026-06-12): Gemini client (`gemini-3.1-flash-lite`, key in `.env`) · boost feature drains the Boost Queue with free-tier throttle (≥4.2 s/request, daily cap in `data\boost_usage.json`) · merge = boost_queue.ai_text + section `boosted` + `[AI Boost]` block appended to jobs.full_text + `ai_boosts` in result.json (no schema change — Shared Store contract intact) · Settings tab (key/model/cap/toggle + Send-now) · auto-boost after each scan when enabled · smoke_boost (offline fake + live) and smoke_gui passing.
- v0.0.2 — inbox watcher + installed-app paths (2026-06-12): polling watcher on `data\inbox` (size-stable pickup, originals → `inbox\processed` / `inbox\failed`, never deleted) · paths now frozen-aware — installed builds use `%LOCALAPPDATA%\OCR-Agentic-Ai\` per Nick ("like a normal program"), `OCR_AGENTIC_DATA_DIR` override · Settings toggle.
- v0.0.3 — local API for Open-Claw (2026-06-12): stdlib REST on `127.0.0.1:8765` — GET `/health` `/jobs` `/jobs/{id}` `/jobs/{id}/result`, POST `/scan` `/boost/run` (endpoint contract = breaking-change surface) · Settings toggle+port · smoke_watcher 4/4, smoke_api 7/7 (incl. live boost drain 6/6), GUI smoke green.
- v0.0.4 — self-introduction handshake (2026-06-12, Nick's request): one machine-readable self-description (identity/role/paths/schema/interfaces/conventions) delivered through both doors — `data\introduction.json` rewritten on every app start + `GET /introduce` · adding fields is safe, renaming/removing is breaking · smoke_api 8/8, GUI smoke green.
- v0.0.5 — PDF input (2026-06-12): pypdfium2 renders each PDF page at 300 DPI (40 MP safety cap) → one Job per page through the unchanged pipeline (`run_source()` dispatcher) · source recorded as `path#page=N`, rendered page saved as `original.png`, result.json gains additive `page`/`pages` fields (schema-safe) · PDF accepted by GUI picker, inbox watcher and `POST /scan` (response gains additive `jobs` list) · introduction updated · smoke_pdf 14/14, all prior smokes green.
- v0.0.6 — tray mode (2026-06-12): closing the window hides to system tray (pystray) while inbox watcher + local API keep serving Open-Claw · tray menu Open/Quit, runtime-drawn eye glyph (real icon asset comes with the .exe build) · `tray_enabled` setting + Settings toggle · clean shutdown stops watcher/API/tray · smoke_tray 6/6, GUI smoke green.
- v0.0.7 — self-rescue engine (2026-06-12, Nick: "raise the limits, make it smart on its own"): adaptive grid (configured grid = minimum, scales with image size, cap 5×5) · dual full-pass (block + sparse PSM 11 for scattered drawing labels) · rescue pass for unclear sections before the Boost Queue — 4× zoom → Otsu binarize (auto-invert dark pages) → sparse → full inversion (light-on-dark) → 90°/270° rotation (vertical labels), stop at first variant clearing the threshold · sections gain additive `rescued`/`rescue_method` fields · new settings `auto_grid`/`rescue_enabled` (both default on) · introduction documents both conventions · smoke_rescue 12/12 (vertical label + white-on-black now read locally), full regression 7/7 green.
- v0.0.8 — deep-detail mode (2026-06-12, Nick: "slower is fine, I want detailed work"): tiles at 3× zoom + dual pass (block+sparse) per tile · adaptive grid cap 7×7 (target tile 800 px) · rescue = deep merge of ALL variants (no early stop, noise floor conf 45, union deduped) triggered below quality bar `rescue_trigger_conf` 75 · PDF 400 DPI / 80 MP cap · upscale_min_side 2000 · ~2-4× slower per scan, accepted · torture-sheet conf 84.9→88.8 · smoke fixes: watcher deadline 300 s, smoke_api no longer sends real Gemini requests (queue cleared in-test — quota leak since v0.0.3 plugged, see bug_v0.0.8) · regression 8/8.
- v0.0.9 — pro distribution + silent auto-update (2026-06-12, Nick: GitHub Releases · Program Files · silent): real multi-res app icon (assets\icon.ico, scanning-eye design — window/taskbar/tray/exe/installer) · single-instance mutex · `src/features/updater/` — daily GitHub-Releases check, SHA-256-verified download, staged install on app exit via detached script (/VERYSILENT + relaunch) · Settings → Updates box (toggle + repo) · Inno Setup 6 installer: fixed AppId (in-place upgrades), bundled Tesseract+tha/eng/osd ({app}\tesseract — zero dependencies for end users), optional desktop icon + all-users autostart, uninstaller asks before deleting the Shared Store · `build\build.ps1` one-command release (icon→CODEMAP→PyInstaller→stage→ISCC→sha256) · output `dist\installer\OCR-Agentic-Ai_Setup_v0.0.9.exe` 68.1 MB, frozen exe verified live (/health v0.0.9, data in %LOCALAPPDATA%) · smoke_updater 9/9 · ADR-001 · open: Nick creates the GitHub repo + sets owner/repo in Settings.
- v0.1.5 — physical-size grid + page orientation + seam pass (2026-06-13, Nick: "the 12-tile grid isn't accurate — split the document properly, not just edges; measure the PDF size before scanning"): **paper-size-aware grid** — `pdfio.page_size_mm()` reads the REAL page size (mm) before every scan; tiles target ~11 cm of paper (`imaging.grid_from_mm`, A4≈2×3 / A3≈3×4 / A0 capped 7×7) so an A4 is no longer shredded into 15 tiny tiles; image files use DPI metadata, pixel heuristic only as last resort; additive `page_size_mm` in result.json · **whole-page orientation** — `engine.detect_rotation()` OCRs a thumbnail at 0/90/180/270 and turns the page upright before everything (empirical — Tesseract OSD guessed wrong on line work); MSBESB page 4 (landscape drawing rotated inside a portrait A3) went from garbage to real text (PANEL/DIESEL GEN/busbar sizes, 259 words ≥90 conf); additive `page_rotation` + overlay viewer rotates to match · **frame-interior grid** — `imaging.frame_interior()` finds the drawing's border frame and grids only INSIDE it (zone-letter strips A/B/C + frame lines never become tiles) · **valley-snapped cuts** — `imaging.smart_cuts()` moves each grid cut to the emptiest nearby column/row so tiles split between content, not through words · **seam pass** — `imaging.seam_strips()` scans a wide strip over every internal cut (Nick: "สแกนระหว่างรอยต่อ"); fragment-buster in `_dedupe` lets the whole word evict its own tile-cut pieces (high-conf "7 7" fragments used to kill "SEAMWORD77") · **section verdicts after stitch** — a tile is queued only if the MERGED words covering it are still poor (seam-solved tiles no longer waste AI quota) · **ruled-only filter** — `imaging.ruled_only()` detects tiles whose ink is straight table/frame lines via projection profiles; empty table cells are no longer queued (page 2: queued tiles 6→1) · dynamic tile zoom (3× capped at 8000 px side) · new smoke_grid 15/15 · full regression green (11 suites).
- v0.1.4 — content-aware grid + border-tile filter + real delete (2026-06-12, Nick saw the installed jobs\ folders: "it keeps scanning document edges — what for? and there is no delete button"): **content-aware Sectioned Scan** — `imaging.content_bbox()` finds the ink bounding box (1% pad) and the grid covers ONLY that area, not the empty paper margin (test PDF: 12→9 tiles, bigger and all on content) · **line-only tile filter** — a tile with no words after every rescue variant and ink <2% is frame/border lines, NOT queued for AI and NO crop saved (blank bar also raised 0.2%→0.4%); torture-sheet flagged sections 2→0, job folders stop filling with corner/line crops · **real delete** — Jobs tab 🗑 Delete (single page or ALL pages of the file via yes/no/cancel, double-confirmed, folder+rows gone) + 🧹 Empty trash on the toolbar purges everything archived; Archive renamed 🗂 and hints at Empty trash · store: `delete_job`/`archived_jobs` · smoke_manage +7 checks (delete/trash/content_bbox) — full regression green.
- v0.1.3 — Thai-noise fix + updater fix + batch resume (2026-06-12, Nick: "reopened, no update; Thai gibberish mixed into results"): **auto language detect** — after the first full pass, a page with almost no real Thai words (<3 words of ≥3 chars AND <2% of confident words) re-runs the whole pipeline English-only, killing the stray-glyph hallucination on line drawings (torture sheet: "ท า ท า" junk gone, smoke_pdf page-2 conf 75.0→79.4); real Thai pages keep tha+eng; new setting `auto_language` (default on) + Settings switch; additive `languages_used` in result.json · **updater checked only once per day** — quit+reopen missed same-day releases (root cause of "ปิดเปิดแล้วไม่อัพ"); now checks on EVERY start + "Check now" button in Settings → Updates (manual check reports up-to-date/offline/staged too) · **batch resume** — `store.done_pages()` + `skip_pages` in run_source; picking a partially-scanned PDF offers "skip N finished pages"; orphaned 'processing' jobs are marked error on app start (`fail_orphans`) · tests: new smoke_lang 8/8 · smoke_manage made idempotent (archives its own leftovers) · pipeline/pdf/gui/manage green.
- v0.1.2 — data-management suite, all 10 functions (2026-06-12, Nick: "จัดมาให้ครบเลย" after "the app feels empty"): Jobs tab rebuilt — jobs **grouped per Source file** (PDF pages collapse, expandable), **full-text search** (text/source/label), **image preview**, **overlay viewer** (word boxes coloured by conf ≥75 green / ≥60 yellow / <60 red, rendered on the preprocessed canvas so boxes align, saved as additive `overlay.png`), **open job/data folder** in Explorer, per-job **label** + **archive** (folder → `jobs\_trash`, moved never deleted, hidden from listings/API), **export** whole Source as .txt/.json + copy-text · new **Dashboard tab** (library stats, batch progress + ETA + pause state, AI Boost budget used/cap, 2 s auto-refresh) · **pause/resume/cancel** running scans (`ScanControl` checkpoints between sections/pages; cancel keeps finished pages) · Shared Store: additive `jobs.label`/`jobs.archived` columns (auto-migration; introduction.json documents them; `/jobs` now hides archived + carries label) · new store APIs (search_jobs/set_label/set_archived/job_words/stats) · tests: new smoke_manage 15/15 (self-scoped per bug_v0.1.1 lesson) + gui/api/pdf/pipeline regression green.
- v0.1.1 — live page streaming + paid-tier unlock + model dropdown (2026-06-12, Nick during the 45-page real run): `run_source()` gains optional `on_page_done` callback — Scan tab now streams each finished PDF page into the result box immediately (was: silent until the whole file finished), Jobs list auto-refreshes, inbox watcher fires per page too (auto-boost no longer waits for the full batch; additive change, contract intact) · Settings: "Unlock paid tier" switch with explicit consent dialog — unlocked = throttle 4.2 s→0.5 s, daily cap skipped, all models; locked (default) = hard-locked to `gemini-3.1-flash-lite` regardless of stored model · model is now a dropdown of the 8 main Gemini models · new setting `paid_tier` (additive) · test-isolation fix: smoke_boost used to fake-answer the WHOLE dev Boost Queue (contaminated jobs 36-37 — cleaned, requeued; see bug_v0.1.1), now drains only its seeded item and asserts the free-model lock · smoke_boost 6/6, smoke_pdf, smoke_watcher, smoke_gui green.
- v0.1.0 — mascot + live update channel + first public release (2026-06-12, carry rule 0.0.9→0.1.0): mascot "Scout" (one-eyed scanner robot, same palette as the icon — `build\make_mascot.py` → `assets\mascot.png`) shown in the Scan tab and on the installer wizard pages (WizardImageFile/WizardSmallImageFile) · public GitHub repo `nicksuksantr-pixel/OCR-Agentic-Ai` created and pushed (.gitignore keeps .env/data/records/ผู้ใช้ local) · default `update_repo` baked in → fresh installs auto-update out of the box · **Release v0.1.0 published with Setup exe + sha256; updater live-verified against the real release** · first real-file test: Nick's MSBESB.pdf (45-page ship electrical drawing) sample pages — conf ~73-76%, ~4 min/page deep-detail, adaptive 24-section grid + all rescue variants firing (incl. rotate90/270 + invert); known issues logged: Thai-glyph noise on line work, dual-pass duplicate words.
