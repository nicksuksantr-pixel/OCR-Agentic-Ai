# V-Log — OCR-Agentic-Ai

> 3-place versioning `vX.Y.Z` per Nick (2026-06-12) · no place exceeds 9, carry instead.

- v0.0.0 — initial project (2026-06-12): kickoff grill → CONTEXT.md · feature-based scaffold · local hybrid-ready pipeline (Tesseract tha+eng, full pass + 3×3 Sectioned Scan + stitch + dedupe) · SQLite Shared Store (jobs/sections/words/boost_queue) · CustomTkinter GUI (Scan + Jobs tabs) · smoke test passing.
- v0.0.1 — AI Boost sender (2026-06-12): Gemini client (`gemini-3.1-flash-lite`, key in `.env`) · boost feature drains the Boost Queue with free-tier throttle (≥4.2 s/request, daily cap in `data\boost_usage.json`) · merge = boost_queue.ai_text + section `boosted` + `[AI Boost]` block appended to jobs.full_text + `ai_boosts` in result.json (no schema change — Shared Store contract intact) · Settings tab (key/model/cap/toggle + Send-now) · auto-boost after each scan when enabled · smoke_boost (offline fake + live) and smoke_gui passing.
- v0.0.2 — inbox watcher + installed-app paths (2026-06-12): polling watcher on `data\inbox` (size-stable pickup, originals → `inbox\processed` / `inbox\failed`, never deleted) · paths now frozen-aware — installed builds use `%LOCALAPPDATA%\OCR-Agentic-Ai\` per Nick ("like a normal program"), `OCR_AGENTIC_DATA_DIR` override · Settings toggle.
- v0.0.3 — local API for Open-Claw (2026-06-12): stdlib REST on `127.0.0.1:8765` — GET `/health` `/jobs` `/jobs/{id}` `/jobs/{id}/result`, POST `/scan` `/boost/run` (endpoint contract = breaking-change surface) · Settings toggle+port · smoke_watcher 4/4, smoke_api 7/7 (incl. live boost drain 6/6), GUI smoke green.
- v0.0.4 — self-introduction handshake (2026-06-12, Nick's request): one machine-readable self-description (identity/role/paths/schema/interfaces/conventions) delivered through both doors — `data\introduction.json` rewritten on every app start + `GET /introduce` · adding fields is safe, renaming/removing is breaking · smoke_api 8/8, GUI smoke green.
- v0.0.5 — PDF input (2026-06-12): pypdfium2 renders each PDF page at 300 DPI (40 MP safety cap) → one Job per page through the unchanged pipeline (`run_source()` dispatcher) · source recorded as `path#page=N`, rendered page saved as `original.png`, result.json gains additive `page`/`pages` fields (schema-safe) · PDF accepted by GUI picker, inbox watcher and `POST /scan` (response gains additive `jobs` list) · introduction updated · smoke_pdf 14/14, all prior smokes green.
- v0.0.6 — tray mode (2026-06-12): closing the window hides to system tray (pystray) while inbox watcher + local API keep serving Open-Claw · tray menu Open/Quit, runtime-drawn eye glyph (real icon asset comes with the .exe build) · `tray_enabled` setting + Settings toggle · clean shutdown stops watcher/API/tray · smoke_tray 6/6, GUI smoke green.
- v0.0.7 — self-rescue engine (2026-06-12, Nick: "raise the limits, make it smart on its own"): adaptive grid (configured grid = minimum, scales with image size, cap 5×5) · dual full-pass (block + sparse PSM 11 for scattered drawing labels) · rescue pass for unclear sections before the Boost Queue — 4× zoom → Otsu binarize (auto-invert dark pages) → sparse → full inversion (light-on-dark) → 90°/270° rotation (vertical labels), stop at first variant clearing the threshold · sections gain additive `rescued`/`rescue_method` fields · new settings `auto_grid`/`rescue_enabled` (both default on) · introduction documents both conventions · smoke_rescue 12/12 (vertical label + white-on-black now read locally), full regression 7/7 green.
- v0.0.8 — deep-detail mode (2026-06-12, Nick: "slower is fine, I want detailed work"): tiles at 3× zoom + dual pass (block+sparse) per tile · adaptive grid cap 7×7 (target tile 800 px) · rescue = deep merge of ALL variants (no early stop, noise floor conf 45, union deduped) triggered below quality bar `rescue_trigger_conf` 75 · PDF 400 DPI / 80 MP cap · upscale_min_side 2000 · ~2-4× slower per scan, accepted · torture-sheet conf 84.9→88.8 · smoke fixes: watcher deadline 300 s, smoke_api no longer sends real Gemini requests (queue cleared in-test — quota leak since v0.0.3 plugged, see bug_v0.0.8) · regression 8/8.
- v0.0.9 — pro distribution + silent auto-update (2026-06-12, Nick: GitHub Releases · Program Files · silent): real multi-res app icon (assets\icon.ico, scanning-eye design — window/taskbar/tray/exe/installer) · single-instance mutex · `src/features/updater/` — daily GitHub-Releases check, SHA-256-verified download, staged install on app exit via detached script (/VERYSILENT + relaunch) · Settings → Updates box (toggle + repo) · Inno Setup 6 installer: fixed AppId (in-place upgrades), bundled Tesseract+tha/eng/osd ({app}\tesseract — zero dependencies for end users), optional desktop icon + all-users autostart, uninstaller asks before deleting the Shared Store · `build\build.ps1` one-command release (icon→CODEMAP→PyInstaller→stage→ISCC→sha256) · output `dist\installer\OCR-Agentic-Ai_Setup_v0.0.9.exe` 68.1 MB, frozen exe verified live (/health v0.0.9, data in %LOCALAPPDATA%) · smoke_updater 9/9 · ADR-001 · open: Nick creates the GitHub repo + sets owner/repo in Settings.
- v0.1.0 — mascot + live update channel + first public release (2026-06-12, carry rule 0.0.9→0.1.0): mascot "Scout" (one-eyed scanner robot, same palette as the icon — `build\make_mascot.py` → `assets\mascot.png`) shown in the Scan tab and on the installer wizard pages (WizardImageFile/WizardSmallImageFile) · public GitHub repo `nicksuksantr-pixel/OCR-Agentic-Ai` created and pushed (.gitignore keeps .env/data/records/ผู้ใช้ local) · default `update_repo` baked in → fresh installs auto-update out of the box · **Release v0.1.0 published with Setup exe + sha256; updater live-verified against the real release** · first real-file test: Nick's MSBESB.pdf (45-page ship electrical drawing) sample pages — conf ~73-76%, ~4 min/page deep-detail, adaptive 24-section grid + all rescue variants firing (incl. rotate90/270 + invert); known issues logged: Thai-glyph noise on line work, dual-pass duplicate words.
